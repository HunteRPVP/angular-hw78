import { FamilyNamesPipe } from './family-names.pipe';

describe('FamilyNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new FamilyNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
